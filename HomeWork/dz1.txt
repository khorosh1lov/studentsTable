1. Вынести логику фильтрации студентов в отдельный filterTable.js
2. Переписать декларативные функции function() {} в стрелочные () => {}
3. Создать вспомогательную функцию create() для createTable() чтобы убрать
повторение по DRY
4. Для createTable() Переписать forEach() на map()
